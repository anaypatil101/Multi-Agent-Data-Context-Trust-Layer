[build]
buildCommand = "pip install -e ."

[deploy]
startCommand = "uvicorn context_layer.api:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
